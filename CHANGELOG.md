# Changelog

本项目所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.0] - 2026-08-09

### 新增

- GUI 悬浮窗增加**像素边长调节**：输入目标像素边长（`0` = 自动按画面中位数），回车或失焦即生效并持久化到配置
- GUI 按 **1:1 直观绘制容忍方块**（最小 / 目标 / 最大），标注实际像素数，自动模式下随检测实时刷新
- GUI 美化：标题栏、状态灯（运行绿 / 暂停橙 / 取色蓝）、深色主题配色
- 配置新增 `PIXEL_SIDE` 字段，像素边长设置重启后保留

### 修复

- **被 UI 遮挡的像素误作识别基准**：面积过滤改用中位数而不是最小值，遮挡残片不再把正常像素全部滤掉（漏画）
- **偶发"粘连画一排"**：收紧拖画合并距离（`CONT_DIST_MAX` 2.5→1.8），并增加宽度一致性检查，被遮挡的窄残片不再混入拖画
- **启动时误画鼠标原位 / 划一刀**：启动、退出、暂停、翻页各路径统一释放空格，且第一个目标像素慢移定位（0.2s）后再画
- **提速后几乎不画**：空格冷却期内不再"假成功"，等够冷却再真按，保证每个目标都画到
- **拖画认不出**：拖画终点增加 `DRAG_HOLD` 按住时间，确保游戏识别"按住拖动"
- 修复全色模式下取色区找不到按钮时用上一颜色误涂（跳过该色）

### 性能

- 移除 `pyautogui` 每个动作后默认 0.1 秒停顿（`pyautogui.PAUSE = 0`），整体提速约 5 倍

### 安全与健壮性

- 配置文件解析由 `eval` 改为 `ast.literal_eval`，不再把配置文件内容当代码执行
- 配置文件固定读取脚本所在目录，不再受启动目录影响

### 其他

- 删除死代码（`CLICK_INTERVAL`、未使用的 `platform` 导入、0 字节的 `git` 残留文件）
- `find_color_in_pick` 循环变量遮蔽改名，降低踩坑风险

## [1.0.0] - 2026-07-27

初始重构发布版：

- 重写检测逻辑，支持全色模式、免费色模式自动遍历颜色库
- 新增自动翻页（WASD 方向）
- 新增取色区定义，自动定位颜色按钮
- 优化矩形合并算法，支持拖画路径
- 精简全局状态，消除多个死锁与竞态 bug
- 添加 GUI 状态窗口，实时显示进度

